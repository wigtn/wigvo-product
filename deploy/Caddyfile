# Automatic HTTPS (Let's Encrypt) for the relay subdomain.
# reverse_proxy passes WebSocket upgrades through transparently.
# Requires: DNS wigvo-product-api.wigtn.com -> VM IP, and ports 80/443 open.
wigvo-product-api.wigtn.com {
	encode zstd gzip
	reverse_proxy relay:8080
}
